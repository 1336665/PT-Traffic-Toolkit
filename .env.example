# PT Manager Pro Environment Configuration

# Security - CHANGE THIS IN PRODUCTION!
SECRET_KEY=change-this-to-a-random-string

# Database
DATABASE_URL=sqlite+aiosqlite:///./data/pt_manager.db

# Debug mode
DEBUG=false

# CORS - In production, set to your frontend domain(s)
# Use comma-separated values for multiple origins
# Examples:
#   CORS_ORIGINS=http://localhost:8080
#   CORS_ORIGINS=https://pt.yourdomain.com
#   CORS_ORIGINS=https://pt.yourdomain.com,http://localhost:8080
# Use "*" to allow all origins (not recommended for production)
CORS_ORIGINS=*

# Telegram notifications (optional)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# HTTP client defaults (optional)
# If you trust your PT sites' SSL certificates, you can set HTTP_VERIFY_SSL=true
HTTP_TIMEOUT=30.0
HTTP_VERIFY_SSL=false
HTTP_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36

# RSS tuning (optional)
# Limit concurrent detail-page requests when ONLY_FREE is enabled
RSS_MAX_CONCURRENT_FREE_CHECKS=8

# Scheduler defaults (optional)
# Prevent overlapping job executions
SCHEDULER_JOB_MAX_INSTANCES=1
SCHEDULER_JOB_COALESCE=true
SCHEDULER_MISFIRE_GRACE_TIME=60
